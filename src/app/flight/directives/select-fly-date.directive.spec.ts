import { SelectFlyDateDirective } from './select-fly-date.directive';

describe('SelectFlyDateDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectFlyDateDirective();
    expect(directive).toBeTruthy();
  });
});
