import { CropAvatarDirective } from './crop-avatar.directive';

describe('CropAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new CropAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
